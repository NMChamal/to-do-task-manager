{
  "name": "to-do-task-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run start --workspace=backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}